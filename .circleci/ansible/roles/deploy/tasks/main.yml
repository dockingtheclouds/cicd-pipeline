---
- name: Create directory
  file:
    path: /home/ubuntu/backend
    state: directory

- name: Copy backend tar.gz file and unzipping it
  become: yes
  unarchive:
    src: ~/project/backend/dist.tar.gz
    dest: /home/ubuntu/backend